CREATE TABLE IF NOT EXISTS BlockedHeyUserEntity (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL UNIQUE
);

getAllBlockedUsers:
SELECT * FROM BlockedHeyUserEntity;

insertBlockedUsers:
INSERT OR IGNORE INTO BlockedHeyUserEntity (id, user_id)
VALUES(?, ?);